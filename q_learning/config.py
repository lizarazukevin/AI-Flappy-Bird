config = {'train': False,  # train or run the model
          'show_game': True,
          'print_score': 1000,
          'max_score': 10000,  # end the episode and update q-table
          'resume_score': 10000,
          }
